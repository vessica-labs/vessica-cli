# Deployment

Document build inputs, environments, migration ownership, readiness checks, rollback behavior, runtime topology, scaling constraints, required secrets, and live verification steps.

Pin toolchains and build actions to immutable versions. A successful build is not a successful deployment: verify the terminal platform status and the live readiness endpoint.
